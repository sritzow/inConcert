
@model inConcert.Models.Notifications

@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{

}

<table class="table" id="notificationsTable">
    <tr>

        <th>
            <h3>Update</h3>
        </th>
        <th>
            <h3>TimeStamp</h3>
        </th>

    </tr>

    @foreach (var item in Model.Update)
    {

        <tr>
            @Html.Partial("NotificationPartialView", item)
        </tr>
    }
</table>

